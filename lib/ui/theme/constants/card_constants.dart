import 'package:flutter/material.dart';

class CardConstants {
  static const horizontalCardSize = Size(140, 280);
  static const borderRadius = Radius.circular(5);
  static const space = 20.0;
  static const horizontalSpace = 10.0;
  
  static const topFirstCardSize = Size.fromHeight(210);
}