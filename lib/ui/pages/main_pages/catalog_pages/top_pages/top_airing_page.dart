import 'package:flutter/material.dart';

class TopAiringPage extends StatelessWidget {
  const TopAiringPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('top airing');
  }
}
