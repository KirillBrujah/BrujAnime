import 'package:flutter/material.dart';

class TopAllPage extends StatelessWidget {
  const TopAllPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Top All');
  }
}
